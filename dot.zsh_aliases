alias l='ls -lah'
alias la='ls -lah $LS_OPTIONS'
alias ll='ls -l $LS_OPTIONS'
alias ls='ls $LS_OPTIONS'
alias lsa='ls -lah'

alias mkae=make
alias mkdri=mkdir

function errno() {
   command grep $1 --color=auto /usr/include/asm-generic/errno-base.h /usr/include/asm-generic/errno.h
}

grep='grep --color=auto -n '
